# -Music-Vibe-Matcher
A content-based music recommender that finds tracks matching a "vibe" (energy, tempo, mood, popularity, loudness) using a cosine-distance k-NN model trained on ~50k Spotify tracks (2020–2025) — no listening history required.
